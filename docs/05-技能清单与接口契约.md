# 05 · 能力清单与接口契约 ★

> 经 skill↔tool 评审后（见 `docs/00-项目结构.md §3`），原 5 个能力按"确定性 / 显式参数 / 结构化结果 / 外部交互"四维划分：**2 个保留为技能**（`dish-recommender`、`nutrition-analyzer`），**3 个迁移为工具**（`canteen-menu-query`、`crowd-forecast`、`feedback-ticket`）。
>
> 其中 **T1 `canteen-menu-query` 为标杆工具**（完整契约与容错实现详见 [agent/tools/canteen-menu-query/README.md](../agent/tools/canteen-menu-query/README.md)），单独满足"至少 1 个独立封装能力"的硬性要求。

| 编号 | 能力 | 类型 | 端口 | 位置 | 状态 | 是否需人工确认 |
| --- | --- | --- | --- | --- | --- | --- |
| **T1** | `canteen-menu-query` | **Tool** | 8201 | `agent/tools/` | 核心 | 否 |
| S1 | `dish-recommender` | Skill | 8101 | `skills/` | 核心 | 否 |
| S2 | `nutrition-analyzer` | Skill | 8102 | `skills/` | 辅助 | 否 |
| **T2** | `crowd-forecast` | **Tool** | 8202 | `agent/tools/` | 辅助 | 否 |
| **T3** | `feedback-ticket` | **Tool**（写） | 8203 | `agent/tools/` | 核心 | **是（LangGraph interrupt）** |

> 端口规划调整：工具使用 8200+ 段，技能使用 8100+ 段，便于网络隔离与排障。

---

## S1 · `canteen-menu-query` 菜单查询（标杆技能）

### 功能
查询校园食堂**真实**的菜单数据：某天 / 某餐次 / 某食堂窗口供应哪些菜，含价格、辣度、主要食材、过敏原标签、余量状态、营业时间。

### 触发示例
- "今天一食堂中午有啥？" / "三楼红烧肉还有吗？" / "15 块以内能吃啥？" / "食堂几点开门？"

### 动作列表

| action | 说明 |
| --- | --- |
| `query_dishes` | 按条件查菜品 |
| `get_dish_detail` | 单菜品详情（配料、过敏原） |
| `get_canteen_info` | 食堂/窗口信息与营业时间 |
| `check_availability` | 指定菜品余量 |

### 入参示例（action=`query_dishes`）

```json
{
  "request_id": "req_01J9Z8X7K2M3N4P5Q6R7S8T9V0",
  "skill": "canteen-menu-query",
  "version": "1.2.0",
  "action": "query_dishes",
  "params": {
    "campus": "东校区",
    "canteen": "第一食堂",
    "date": "2026-09-08",
    "meal_period": "lunch",
    "keywords": ["鸡肉"],
    "filters": {
      "price_max": 15,
      "spice_level_max": 1,
      "tags_include": ["高蛋白"],
      "allergen_exclude": ["花生"],
      "available_only": true
    },
    "sort": { "field": "price", "order": "asc" },
    "pagination": { "page": 1, "page_size": 20 }
  },
  "context": { "user_id_hash": "u_9f2c1a", "trace_id": "tr_01J9Z", "deadline_ms": 800 }
}
```

**参数约束**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `date` | string(date) | ❌ | 缺省 = 今天；仅支持今天 ~ +7 天，超出返回 `40001` |
| `meal_period` | enum | ❌ | `breakfast`/`lunch`/`dinner`/`night_snack`；缺省 = 按当前时间推断 |
| `filters.allergen_exclude` | string[] | ❌ | **硬过滤**，命中即不返回；枚举：花生/海鲜/牛奶/鸡蛋/麸质/大豆/坚果 |
| `filters.spice_level_max` | int 0-3 | ❌ | 0 不辣 / 1 微辣 / 2 中辣 / 3 特辣 |
| `filters.available_only` | bool | ❌ | 默认 true，过滤售罄 |

### 出参示例（成功）

```json
{
  "request_id": "req_01J9Z8X7K2M3N4P5Q6R7S8T9V0",
  "code": 0,
  "message": "ok",
  "data": {
    "canteen": {
      "campus": "东校区", "name": "第一食堂", "floor": 3,
      "open_hours": { "lunch": "10:30-13:00" },
      "status": "open"
    },
    "dishes": [
      {
        "dish_id": "d_10087",
        "name": "香煎鸡胸饭",
        "window": "3F-轻食窗口",
        "price": 12.0,
        "spice_level": 0,
        "tags": ["高蛋白", "低脂"],
        "allergens": [],
        "may_contain": ["大豆"],
        "availability": "in_stock",
        "updated_at": "2026-09-08T11:32:00+08:00"
      }
    ],
    "total": 3,
    "page": 1
  },
  "meta": {
    "latency_ms": 86,
    "skill_version": "1.2.0",
    "data_version": "menu_2026-09-08_v3",
    "source": "cache",
    "degraded": false,
    "cache_hit": true,
    "warnings": ["已为你排除含花生及可能含花生的菜品"]
  }
}
```

### 出参示例（降级：上游超时，走缓存）

```json
{
  "request_id": "req_01J9Z8X7K2M3N4P5Q6R7S8T9V1",
  "code": 0,
  "message": "ok（数据来自缓存，可能非实时）",
  "data": { "dishes": ["..."], "total": 3 },
  "meta": {
    "latency_ms": 812,
    "source": "cache",
    "degraded": true,
    "cache_hit": true,
    "data_version": "menu_2026-09-08_v3",
    "cached_at": "2026-09-08T11:42:10+08:00",
    "warnings": ["上游菜单服务超时，当前为 60 秒前缓存，实际以窗口现场为准"]
  }
}
```

### 出参示例（失败）

```json
{
  "request_id": "req_01J9Z8X7K2M3N4P5Q6R7S8T9V2",
  "code": 50420,
  "message": "菜单服务暂时不可用，已尝试缓存与快照均未命中。建议直接到食堂窗口查看，或稍后再问。",
  "data": null,
  "meta": { "latency_ms": 950, "degraded": true, "source": "none", "warnings": ["实时数据不可用"] }
}
```

### 降级链与 SLA

| 级别 | 数据源 | 触发条件 | 用户可见提示 |
| --- | --- | --- | --- |
| L0 | 上游菜单 API | 正常 | 无 |
| L1 | Redis 缓存（TTL 60s） | 上游超时/5xx | "数据为 60 秒前" |
| L2 | 昨日同餐次快照 | 缓存未命中 | "参考昨日菜单，今日可能有调整" |
| L3 | ChromaDB 向量召回 | 快照缺失 | "以下为历史/文档信息，可能不准确" |
| L4 | 兜底话术 | 全部失败 | "服务暂不可用，请到现场查看"（`code=50420`） |

- P95 延迟 ≤ 500ms（L0/L1）；可用性 ≥ 99.5%
- 超时 800ms，重试 2 次（仅读），熔断阈值：10s 内错误率 > 50% 且 ≥20 请求

---

## S2 · `dish-recommender` 个性化推荐

| 项 | 内容 |
| --- | --- |
| 功能 | 在给定约束（预算、忌口、过敏原、营养目标、口味）下，从候选菜品中给出 **1 主菜 + 1 素菜 + 1 主食（+汤）** 的组合推荐，并给出推荐理由 |
| 触发 | "15 块吃啥好"、"减脂推荐一个"、"换换口味" |
| 依赖 | 内部调用 S1 获取候选（通过服务间 HTTP，主智能体不感知） |

### 入参

```json
{
  "action": "recommend_combo",
  "params": {
    "campus": "东校区", "canteen": "第一食堂", "date": "2026-09-08", "meal_period": "lunch",
    "budget": 15,
    "preferences": { "taste": ["清淡"], "avoid": ["香菜", "内脏"], "allergens": ["花生"] },
    "goals": { "type": "fat_loss", "calorie_max": 650 },
    "diversity": { "exclude_dish_ids": ["d_10087"], "reason": "最近已吃过" },
    "top_k": 3
  }
}
```

### 出参（节选）

```json
{
  "code": 0,
  "data": {
    "combos": [
      {
        "rank": 1,
        "items": [
          { "dish_id": "d_10087", "name": "香煎鸡胸饭", "price": 12.0, "role": "main" },
          { "dish_id": "d_10233", "name": "清炒时蔬", "price": 3.0, "role": "veg" }
        ],
        "total_price": 15.0,
        "nutrition_estimate": { "kcal": 610, "protein_g": 38, "fat_g": 14 },
        "reasons": ["蛋白质 38g 达单餐目标", "总价刚好 15 元", "已排除花生与香菜"],
        "score": 0.91
      }
    ]
  },
  "meta": { "source": "realtime", "degraded": false }
}
```

### 容错
- 候选为空（预算过低/过滤过严）→ `code=40404` + `message` 建议放宽条件（"最低可行预算约 12 元"）
- S1 不可用时，降级为**基于 ChromaDB 历史菜单的近似推荐**，`degraded=true` + 提示"参考历史数据"
- 求解为启发式束搜索，超时 300ms 直接返回当前最优解（保证有结果）

---

## S3 · `nutrition-analyzer` 营养分析

| 项 | 内容 |
| --- | --- |
| 功能 | 估算菜品/组合的能量与三大营养素，对照《中国居民膳食指南》给出**参考性**建议 |
| 触发 | "这顿多少卡"、"帮我看看蛋白质够不够" |
| 知识 | 内置食物成分表 + RAG 补充（ChromaDB `nutrition_knowledge`） |

### 入参

```json
{
  "action": "analyze",
  "params": {
    "items": [
      { "name": "香煎鸡胸饭", "dish_id": "d_10087", "portion": "1份" },
      { "name": "清炒时蔬", "dish_id": "d_10233", "portion": "1份" }
    ],
    "user_profile": { "gender": "male", "age": 20, "weight_kg": 70, "activity_level": "moderate" },
    "goal": "fat_loss"
  }
}
```

### 出参

```json
{
  "code": 0,
  "data": {
    "totals": { "kcal": 610, "protein_g": 38.2, "fat_g": 14.5, "carb_g": 78.0, "sodium_mg": 1120 },
    "reference": { "recommended_kcal_per_meal": 700, "protein_target_g": 35 },
    "evaluation": [
      { "dim": "energy", "level": "ok", "text": "能量 610 kcal，低于单餐参考 700 kcal，符合减脂节奏" },
      { "dim": "sodium", "level": "high", "text": "钠含量偏高，建议搭配清淡汤品或减少酱汁" }
    ],
    "confidence": "medium",
    "disclaimer": "REQUIRED_NUTRITION_DISCLAIMER"
  },
  "meta": { "degraded": false, "source": "成分表+估算模型" }
}
```

### 容错与合规
- 菜品不在成分表 → 用同类菜品中位值估算并置 `confidence=low` + `meta.estimation_note`
- **出参必须带 `disclaimer`**，主智能体出站时强制展开为"以上为估算值，非医疗建议"
- 涉及疾病（糖尿病、肾病、痛风）→ 拒绝给具体饮食处方，只给通用原则 + 建议咨询校医院/营养师

---

## S4 · `crowd-forecast` 人流预测

| 项 | 内容 |
| --- | --- |
| 功能 | 预测各窗口未来 30/60 分钟的排队时长，给出错峰建议 |
| 触发 | "哪个窗口人少"、"现在去要不要排队" |
| 数据源 | 历史刷卡/支付流水（脱敏聚合）+ 实时在店人数估计 |

### 入参

```json
{
  "action": "forecast",
  "params": {
    "campus": "东校区", "canteen": "第一食堂",
    "datetime": "2026-09-08T11:50:00+08:00",
    "horizon_minutes": 30
  }
}
```

### 出参（节选）

```json
{
  "code": 0,
  "data": {
    "windows": [
      { "window": "3F-轻食窗口", "queue_minutes": 2, "level": "low" },
      { "window": "1F-大锅菜", "queue_minutes": 12, "level": "high" }
    ],
    "best_time_to_go": "12:20 后预计回落",
    "advice": "现在 3 楼轻食窗口基本不用排队"
  },
  "meta": { "source": "model_v2", "degraded": false, "confidence": 0.78 }
}
```

### 容错
- 实时流水缺失 → 用"同星期几 + 同时间段"历史基线（L2 降级，`degraded=true`）
- 置信度 < 0.6 时，回答中必须带"预测仅供参考"
- **不输出任何个人消费明细**，只输出聚合值（隐私合规）

---

## S5 · `feedback-ticket` 投诉建议工单（写操作）

| 项 | 内容 |
| --- | --- |
| 功能 | 将用户投诉/建议/报修结构化为工单并送审 |
| 触发 | "我要投诉"、"建议增加素食窗口"、"空调坏了" |
| 特殊 | **必须经 LangGraph `interrupt` 人工确认后才真正提交** |

### 入参

```json
{
  "action": "create_ticket",
  "params": {
    "idempotency_key": "req_01J9Z...",
    "category": "complaint",
    "sub_category": "service_attitude",
    "campus": "东校区", "canteen": "第一食堂", "window": "3F-轻食窗口",
    "occurred_at": "2026-09-08T11:30:00+08:00",
    "description": "窗口工作人员态度生硬",
    "urgency": "normal",
    "contact": { "phone_masked": "138****5678", "preferred_channel": "phone" },
    "attachments": []
  }
}
```

### 出参

```json
{
  "code": 0,
  "data": {
    "ticket_id": "TK20260908000123",
    "status": "pending_confirm",
    "preview": "工单预览：类别=服务态度投诉 / 地点=东校区第一食堂3F轻食窗口 / 时间=2026-09-08 11:30",
    "estimated_response_hours": 24
  },
  "meta": { "degraded": false, "warnings": ["该工单需用户确认后提交"] }
}
```

### 容错与合规
- 幂等：`idempotency_key` 相同则直接返回首次结果，防重复提交
- 写操作**不重试**，失败返回具体原因（如"缺少联系方式"）交由主智能体追问
- 联系方式落库前脱敏存储，仅工单处理人可见
- 涉及"食物中毒""异物"等关键词 → 自动置 `urgency=high` 并同步通知食堂值班人（人工告警通道）
- 用户取消 → `action=cancel_ticket`，状态置 `cancelled`

---

## 附：技能 → 场景覆盖矩阵

| 场景 | S1 | S2 | S3 | S4 | S5 | RAG |
| --- | :-: | :-: | :-: | :-: | :-: | :-: |
| 今日菜单查询 | ● | ○ | | | | ○ |
| 预算内推荐 | ● | ● | | | | |
| 过敏/忌口过滤 | ● | ● | | | | ● |
| 营养咨询 | ○ | ○ | ● | | | ● |
| 排队错峰 | | | | ● | | |
| 规章/政策/FAQ | | | | | | ● |
| 投诉建议 | | | | | ● | ○ |
| 食品安全事件 | ○ | | | | ● | ● |

（● 主要依赖，○ 辅助）
